import { LeftSectionDirective } from './left-section.directive';

describe('LeftSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftSectionDirective();
    expect(directive).toBeTruthy();
  });
});
