import { TopSectionDirective } from './top-section.directive';

describe('TopSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TopSectionDirective();
    expect(directive).toBeTruthy();
  });
});
