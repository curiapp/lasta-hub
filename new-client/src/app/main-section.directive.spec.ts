import { MainSectionDirective } from './main-section.directive';

describe('MainSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MainSectionDirective();
    expect(directive).toBeTruthy();
  });
});
