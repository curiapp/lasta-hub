import { MainTopSectionDirective } from './main-top-section.directive';

describe('MainTopSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MainTopSectionDirective();
    expect(directive).toBeTruthy();
  });
});
