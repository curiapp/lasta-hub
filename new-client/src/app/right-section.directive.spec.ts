import { RightSectionDirective } from './right-section.directive';

describe('RightSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RightSectionDirective();
    expect(directive).toBeTruthy();
  });
});
