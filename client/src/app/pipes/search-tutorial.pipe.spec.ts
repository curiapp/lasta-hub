import { SearchTutorialPipe } from './search-tutorial.pipe';

describe('SearchTutorialPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTutorialPipe();
    expect(pipe).toBeTruthy();
  });
});
